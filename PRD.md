# Product LingLing — PRD

## 产品概述

Product LingLing 是一个 AI 产品机会分析工具。

每天自动订阅 AI HOT 精选 RSS feed，从当日精编的行业信号中，由 DeepSeek 识别并生成产品机会卡。每张卡包含用户痛点、AI 解决方式、MVP 方案、风险与验证路径，并附带原文信息源链接，沉淀为可检索的机会卡片库。

## 解决的问题

AI 行业不缺信息，缺的是把信息转化成产品判断的能力。

**目标用户的典型困境：**
1. 每天看到大量 AI 新闻、产品发布、开源项目，但不知道它意味着什么。
2. 看不出信息背后对应哪些用户痛点。
3. 难以判断一条信号是否能转化成产品机会。
4. 看过的信息无法沉淀，下次分析还要从零开始。
5. 需要持续输出 AI 产品观点，但缺少结构化方法。

**核心价值：** 不是帮用户看更多信息，而是帮用户把一条信息分析成一张可以讨论、验证和沉淀的产品机会卡。

## 目标用户

| 用户类型 | 典型场景 |
|---------|---------|
| AI 产品经理 | 每日跟踪行业动态，筛选值得深入分析的方向 |
| 独立开发者 / 创业者 | 寻找可落地的 AI 产品机会 |
| 产品从业者 | 竞品分析、方向研究、产品立项前的信息收集 |

## 产品功能

### 模块一：RSS 自动订阅与机会卡生成

系统每天定时拉取 **AI HOT 精选** RSS feed（`https://aihot.virxact.com/feed.xml`），获取当日约 50 条精编行业信号。

**为什么选这个信源：**
- AI HOT 精选已完成人工筛选，信噪比在同类 AI 信息源中最高。
- 提供标准 RSS 格式，接入成本低。
- 全量流信噪比低，日报粒度太粗，不适合按条分析。

每天生成 **4 张机会卡**：
- 4 张是"5 分钟可以看完"的量，不造成信息过载。
- 从 50 条中选 4 条，保证每张卡的质量。
- 按 4 张/天 × 250 工作日估算，一年可积累约 1000 张机会卡。

每张卡关联原始信息源标题和 URL，方便溯源。

### 模块二：产品机会卡

每张机会卡包含以下字段：

| 字段 | 说明 |
|------|------|
| 机会标题 | 一句话概括产品方向 |
| 用户是谁 | 目标用户画像 |
| 用户痛点 | 真实场景中的具体困难 |
| 为什么 AI 能解决 | AI 在这个场景中的独特优势 |
| MVP 方案 | 最小可行产品的定义 |
| 机会评分 | 6 维度加权评分（满分 10 分），含各维度得分与理由 |
| 主要风险 | 大厂覆盖、商业化、技术等风险 |
| 下一步验证 | 具体可执行的验证动作 |

**6 维度评分体系：**
1. 痛点真实性
2. AI 适配度
3. 市场规模
4. 竞争强度
5. 技术可行性
6. 商业化潜力

### 模块三：机会库

保存历史机会卡，支持：
- 关键词搜索
- 按方向筛选（Agent、AI 办公、AI 教育、AI 营销、AI 编程等）
- 查看机会卡详情与原文链接

第一版使用 SQLite 关键词搜索。后续接入 Chroma 实现语义检索。

## 技术方案

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| 前端 | 单文件 HTML | 基于现有原型，直接 fetch 后端 API |
| 后端 | FastAPI (Python) | RSS 解析、DeepSeek 调用、数据存储 |
| 数据库 | SQLite | 轻量，无需额外部署 |
| 大模型 | DeepSeek API | 结构化分析与评分生成 |
| 向量库 | Chroma（第二阶段） | 语义检索 |
| 部署 | 轻量云服务器 | 4 核 4GB，Docker Compose + Nginx |

**数据流：**

```
定时任务（每日 08:30）
→ 拉取 AI HOT 精选 RSS feed
→ DeepSeek 批量分析，按 6 维度评分筛选
→ 生成 4 张机会卡（含原文链接）
→ 保存到 SQLite
→ 前端展示今日机会卡 + 历史机会库
```

## MVP 范围

### 做
- RSS 自动订阅（AI HOT 精选单一信源）
- DeepSeek 结构化分析与机会卡生成
- 机会卡展示（6 维度评分、原文链接）
- 机会库（关键词搜索、方向筛选）
- 每日定时任务

### 不做
- 多信息源采集
- AI 热点日报（内容生成，非机会分析）
- 用户系统 / 权限系统
- 团队协作
- 多模型路由
- 本地部署大模型
- 复杂 Dashboard

## 产品长处

- 场景聚焦：核心价值是"信号→机会"的转化，不是信息聚合。
- 信源质量有保证：AI HOT 精选已人工筛选，不需要自建爬虫和去重逻辑。
- 结果结构化：每张卡字段一致，可横向比较、可积累。
- 可溯源：每张卡关联原文链接，不是黑盒输出。
- 工程轻量：适合单台低配云服务器部署。

## 已知局限

- AI 生成的机会判断存在主观性，需要人工校正。
- 信源单一，覆盖范围受限于 AI HOT 精选的选题偏好。
- 机会评分无法直接证明市场需求和付费意愿。
- 信源质量依赖 AI HOT 精选的持续运营，存在外部依赖风险。
