# Product LingLing — V3 重设计方案「每日开卡」

> 状态：设计定稿，待实现
> 定稿日期：2026-07-03
> 历史：本文件原为 v1→v2 后端重构方案（信息源池 / 双卡类型 / 聚类），已全部实现并沉淀进 PRD.md。原方案「后续创意」中的盲盒开箱动画，正是 V3 的起点。

---

## 一、为什么要 V3

### v2 的两个结构性问题

1. **惊喜是一次性的。** 空间纵深 + 3D 穿层第一次看很惊艳，但它是固定动画——第二天开始就是重复。装饰性的 wow 不可重复。
2. **三个交互支柱全是移动端的天敌。** hover 浮起（无 hover）、滚轮穿层（无滚轮）、3D perspective transform（性能差）。v2 在手机上基本不可用。

### V3 的核心判断

**可重复的惊喜只能来自内容驱动的变化。** LingLing 每天有别人没有的素材：两张不同的卡、不同的评分、不同的碰撞组合、50 条信号的不同聚类分布。V3 把这些变量变成每天不同的体验，而不是做一个更炫的固定动画。

---

## 二、设计基调（已定稿）

| 维度 | 决策 |
|------|------|
| 交互基调 | **每日开卡仪式**——卡背翻牌 + 评分滚动揭晓 + 跨界碰撞动画 |
| 视觉基调 | **浅色刊物风**——米白底 + 琥珀点缀，像每天拆一份晨报里的签 |
| 技术载体 | **单文件响应式 HTML**（v3.html），与 v2 并存，复用现有后端 API |

隐喻：**每天一期的机会日刊 × 开卡。** 打开即是今天这一期：期号、日期、两张待翻的卡。翻牌是每天的核心仪式，刊物感来自排版，惊喜感来自内容。

---

## 三、惊喜系统（三层，全部由数据驱动）

### 3.1 开卡仪式（每天必然发生、每天不同）
- 今日两张卡以**卡背**形态呈现（卡背印当日期号 + 种子纹理）
- 点按翻牌：3D flip（单卡片级别的 transform，移动端无压力）
- 评分数字从 0 **滚动到位**，数字仍是最大视觉焦点（延续 v2 遗产，最大字号保留）
- 跨界卡翻开时：两个领域词从屏幕两侧飞入、相撞、迸出琥珀色粒子
- 两张都翻开后：出现「今日碰撞」连线 + 当日动态注释
- 当日已翻过的卡，当天再次打开保持翻开状态（localStorage 记录），次日重置

### 3.2 种子生成视觉（每期专属，永不重复）
- 用当日 50 条信号的**聚类分布做种子**（各簇条数、簇间距离、最高信号分），生成当期专属的琥珀色星点带/纹理
- 出现位置：卡背图案、页头装饰带、分享图背景
- 不是随机——是「今天的行业长这样」，有含义的独一无二
- 算法：简单确定性伪随机（以日期+聚类向量为 seed），纯 Canvas/SVG，无依赖

### 3.3 数据彩蛋（低成本、高频率的小新鲜）
| 触发条件 | 表现 |
|---------|------|
| 评分 ≥ 8.5 | 稀有卡待遇：翻开时描边闪光一次（仅一次，不循环） |
| 当日碰撞距离为本月最远 | 注释徽标「本月最远碰撞」 |
| 连续访问 N 天 | 页头小火花「连续第 N 天」（localStorage） |
| 机会库累计满 50/100/200 张 | 期号旁里程碑标记 |
| 每期 | 可导出一张「机会签」分享图（卡片+评分+种子纹理，Canvas 生成 PNG） |

---

## 四、信息架构与页面结构

### 移动端（单列流，纵向滚动）
```
① 刊头     期号 · 日期 · 连续天数 · 种子装饰带
② 今日开卡  两张卡背纵向排列 → 翻牌 → 碰撞注释
③ 机会库    「✦ 适合我」「▲ 本周 TOP」入口 + 历史卡瀑布流（按日期分组）
④ 信号源    当日信号横滑带状图（2D，替代 v2 星图），点开单条详情
```

### 桌面端（同一 HTML，CSS 断点切换）
```
① 刊头     同上，横向布局
② 今日开卡  双卡并置 + 碰撞线居中
③ 机会库    网格布局（3-4 列），筛选器同 v2（类型/评分/搜索/日期）
④ 信号源    带状图加宽，hover 显示信号摘要（hover 仅增强）
```

### 卡片详情（两端一致）
- 移动端 bottom sheet 上滑 / 桌面端居中 Modal
- 完整迁移 v2 已有能力：6 维评分条、动手前体检、开工提示词一键复制、来源链接

---

## 五、交互细则

| 交互 | 规则 |
|------|------|
| 翻牌 | 点按触发；600ms flip；可点「全部翻开」跳过仪式 |
| 评分滚动 | 800ms ease-out 数字滚动，与 flip 衔接 |
| 碰撞动画 | 仅跨界卡；两领域词飞入 400ms + 粒子 600ms；reduced-motion 时直接显示结果 |
| hover | 一律只做增强（阴影/摘要预览），所有功能必须可点按触达 |
| 滚动 | 原生纵向滚动，不劫持；无滚轮穿层 |
| 触控目标 | 最小 44×44px |
| prefers-reduced-motion | 关闭翻牌/粒子/滚动数字，直接呈现内容 |
| 键盘 | 卡片可 Tab 聚焦，Enter 翻牌，Esc 关详情 |

---

## 六、视觉规范

| 项 | 规范 |
|------|------|
| 底色 | 米白 `#FAF8F3`（日间）；正文深墨 `#2C2A26` |
| 主色 | 琥珀 `#B8860B` 系（延续品牌），跨界卡辅以赤陶 `#C4572E` |
| 相关卡 | 琥珀标签 ◎；跨界卡：赤陶标签 ⚡ |
| 字体 | 系统字族；刊头与评分数字用衬线/展示体层级拉开刊物感 |
| 评分数字 | 移动端最大 64px / 桌面端 84px，可变字号随分值 |
| 排版 | 大留白、细分隔线（0.5px）、期刊式栏目标题（小字号+字距） |
| 圆角 | 卡片 16px；控件 10px |
| 阴影 | 极轻（翻牌瞬间短暂加深），无常驻重阴影 |

---

## 七、技术方案

| 项 | 方案 |
|------|------|
| 载体 | `frontend/v3.html` 单文件（HTML+CSS+JS），与 v2.html 并存互不影响 |
| 断点 | 移动 <768px / 桌面 ≥768px，CSS media query + 少量 JS 分支 |
| 后端 | **零改动**。复用 `/api/cards/today`、`/api/cards`、`/api/sources`、kickoff 字段 |
| 种子数据 | 前端从 `/api/sources` 当日条目自行计算聚类摘要做种子（不加接口） |
| 本地状态 | localStorage：当日翻牌状态、连续天数、匹配选择器偏好 |
| 分享图 | Canvas 绘制导出 PNG，无第三方库 |
| 演示模式 | 与 v2 相同：无后端时 Mock 数据自动启动 |
| 静态挂载 | 后端 StaticFiles 已挂载 frontend/，v3.html 放入即可访问 `/v3.html` |

---

## 八、实施步骤

| 阶段 | 内容 | 预估 |
|------|------|------|
| P1 | 骨架：响应式布局 + 刊头 + 数据加载/演示模式 + 卡片详情迁移 | 中 |
| P2 | 开卡仪式：卡背/翻牌/评分滚动/碰撞动画/翻牌状态记忆 | 中 |
| P3 | 机会库 + 信号带状图 + 迁移「适合我」「本周 TOP」 | 中 |
| P4 | 种子生成视觉（卡背纹理/装饰带）+ 数据彩蛋 | 中 |
| P5 | 机会签分享图 + 无障碍细节 + 双端真机验收 | 小 |

每阶段结束 v3.html 均可独立打开验收。

---

## 九、明确不做

- 不改后端、不加接口、不动数据库
- 不做用户系统 / 推送通知（PWA 留待 V3.1 评估）
- 不做深色模式（浅色刊物风优先做透；深浅自适应留待后续）
- 不删 v2.html（保留为桌面端基线，V3 稳定后再讨论去留）
